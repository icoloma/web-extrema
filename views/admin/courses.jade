h1 #{title}
-var courses = items
table.table.table-striped
  thead
    tr
      th
      th Name
  tbody
  -for (var i in courses)
    tr
      td
        img(src="/courses/"+courses[i]._id+"/thumb").thumb-list
      td 
        a(href="/courses/"+courses[i]._id+"/edit") !{courses[i].name}
a(href="/courses/new").btn New

a(href="/admin").btn Back

